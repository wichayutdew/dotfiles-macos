---
description: Estimate token usage and savings from caveman-style compression this session
argument-hint: "[--share]"
---
Load the `caveman-stats` skill (~/.agents/skills/caveman-stats/SKILL.md). Note: pi has no session-log hook like Claude Code's — estimate token savings from this conversation's turns instead of reading a log file. Report estimated tokens saved vs normal verbosity, using the skill's methodology. $ARGUMENTS
