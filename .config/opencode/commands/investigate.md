---
description: Investigate a feature or behavior without changing it
agent: plan
---

Investigate $ARGUMENTS read-only. Use one bounded `explore`, `scout`, or `investigator`; use a second only for an independent evidence stream. Verify current behavior, existing patterns, dependencies, relevant history, and current primary docs. Return `FACT source`, `HYPOTHESIS confidence + falsifier`, `UNKNOWN next check`, and `CHECKED scope`. Do not create code, tickets, documents, or messages.
